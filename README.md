Test the provider code again contract:

In `mb-demo-server` folder, run:

```
mvn test
```

Install the stub locally:

In `mb-demo-server` folder, run:

```
mvn install
```

Test the client against contracts:

In `mb-demo-client` folder, run:

```
mvn test
```

